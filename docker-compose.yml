version: "3.9"

services:
  app:
    build:
      context: RodionProject
      dockerfile: ServerDockerfile


  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
